# Rust
/target/
**/*.rs.bk

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Environment
.env
.env.local

# Data
data/
*.db
*.db-journal
*.sqlite

# Logs
logs/
*.log

# Test artifacts
tts_*.aiff

# ONNX model files
/models/

# Config (may contain secrets)
config.yaml
config.json
!config.example.yaml

# Desktop updater signing keys (minisign). The private key MUST stay local;
# only its base64 value goes into tauri.conf.json + the GitHub secret.
syscity.key
syscity.pub

# Node.js / pnpm
node_modules/

# Root-level workspace artifacts (not the actual project)
/package.json

# Claude Code internal state
.claude/

# Chat UI build output
/dist/

# Chat UI source dependencies
web/node_modules/

# Local development env with sensitive data
scripts/.env

*status.md

output.html

# Eval runtime artifacts (auto-generated, can be cleaned with run.sh clean)
evals/report.md
evals/badcases/
evals/actions/

lcov.info


# Local secret material (never commit)
.tavily-key

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
!.env.example

*.secret
.e2e-tmp/

*.local.md
