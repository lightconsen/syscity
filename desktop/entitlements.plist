<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>com.apple.security.device.audio-input</key>
	<true/>
	<!-- Hardened runtime (applied when the bundle is Developer ID signed)
	     blocks JIT by default. The `plugins` feature runs wasmtime, whose
	     Cranelift backend needs MAP_JIT executable memory. -->
	<key>com.apple.security.cs.allow-jit</key>
	<true/>
</dict>
</plist>
